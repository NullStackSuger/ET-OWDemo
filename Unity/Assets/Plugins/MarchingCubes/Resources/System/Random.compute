// 伪随机数生成函数，基于输入的整数坐标和种子
int Random(int3 p, int seed = 0)
{
    int h = dot(p, int3(1, 57, 113));  // 使用不同的常数混合
    h = (h ^ (h >> 16)) * 0x45d9f3b;
    h = (h ^ (h >> 16)) * 0x45d9f3b;
    h = h ^ (h >> 16);  // 哈希运算

    // 使用种子来影响哈希结果
    h = h + seed;
    
    // 返回 0-15 之间的值，用于从梯度表选择
    return h & 15;
}
